pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo "GitHub code checkout successful"
            }
        }
        stage('Verify Files') {
            steps {
                sh 'ls -l'
            }
        }
    }
}

